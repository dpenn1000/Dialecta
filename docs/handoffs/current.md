# Current state

*Rolling note. Each session appends; the Stop hook adds a stub line automatically. Trim when it passes 60 lines and roll the history into a dated handoff.*

- 2026-09-19, branch `main` (not yet pushed): monorepo scaffold landed (`apps/web`, `packages/core` with 25 tests, `supabase/migrations/20260919000000_foundation.sql`, `.claude/` agents, skills, hooks, `docs/plans/`). Next: commit, push, create `dialecta-staging` Supabase project, apply the foundation migration, set Vercel root to `apps/web`. See `docs/plans/backlog.md` P0.

- 2026-09-19 (later): Dan decided ADR-001 (leave Ghost), ADR-002 (Supabase Auth), ADR-003 (own editor). App no longer reads Ghost; migration 0002 adds native article columns and the `article-media` bucket; `scripts/import-ghost.mjs` imports the five posts once; TipTap installed, editor lands in A-10. New agent `decider` and skill `/dialecta-decide`. Next open decisions: P0-D2, A-D3.

- 2026-09-19 (council): added advisors `treasurer`, `designer`, `philosopher` with charters and research trees under `council/`, `/dialecta-council` protocol, `decider` as chair, `tools/local-research` MCP server (Ollama) wired in `.mcp.json`. First council question to run: P0-D2. Setup for local research: `ollama pull nomic-embed-text` and `ollama pull qwen2.5:14b`, then `npm run index -w tools/local-research`.

- 2026-09-19 (install): `scripts/install-studio-pc.ps1` (one-shot: unzip, npm, tests, Ollama, index, branch, commit, push), `scripts/research-sprint.ps1` (headless `/dialecta-research`), `/dialecta-research` skill, seed reading lists per advisor (verify before filing). Full handoff: `docs/handoffs/dialecta-handoff-2026-09-19-studio-pc.md`.

- 2026-09-19 (agent team): PR #3 merged as `47fab54`, closing P0-1. Added `team/` for the six working agents, mirroring `council/`: `brief.md`, `practices.md`, `knowledge/`. Added `exchange/`, the shared record for handoffs, blind spots, advice and votes, with `SCHEMA.md`, a worked example and an append-only ledger. `/dialecta-research` and `research-sprint.ps1` now take any of the nine agents, plus `council`, `team` or `all`. `.voiceignore` scopes the CI voice gate off the April to May import, 1,667 hard hits across 43 files, none of them in code this project authored. Nine training sessions were started, one per agent, each in its own worktree. Corrected: `dialecta.vercel.app` serves a May 2026 build from `dpenn1000/dialecta-api`, not this repo. Next: Supabase `dialecta-staging` and P0-3, both Dan's.

- 2026-09-19 (supabase reality): P0-2 does not hold as written. The live Dialecta project is `mguulnibvzusfvyuowwh` in the Pennington Media Group org, with 32 tables and 20 applied migrations from April and May 2026, and 10 of the 13 tables this repo's migrations create already exist there with rows. Generated `supabase/types.ts` from live, repointed the broken `npm run types` (it passed `--local`, which needs Docker, and called a `supabase` binary that is not installed). Created nothing, applied nothing, deleted nothing. Full detail in `dialecta-handoff-2026-09-19-supabase-reality.md`; the open question is `exchange/open/2026-09-19-001`.

- 2026-09-19 (builder training): research sprint on `builder`, training only, nothing built. Six reading-list leads verified against primary sources and filed to `team/builder/knowledge/`, none dead. Four carry a correction where the lead's reasoning was wrong rather than its source: the React 19 Action covers the comment insert and not a tier that arrives later; `@supabase/ssr` now says `getClaims()` rather than the `getUser` against `getSession` framing, and `setAll` takes two arguments; on Next 15 nothing is cached by default, so a stale thread is a missing revalidate; and there is no migration 0002, the `article-media` bucket lives in `20260919000100_articles_native.sql` with a select policy and no insert policy. A seventh note records what A-1 requires. `practices.md` goes from five rows all citing the mandate to thirteen, nine citing a filed note, and no row now cites the mandate alone. One practice moved on evidence: "Classification never blocks the comment insert in the request path" is marked contested, because `docs/Dialecta_Discourse_Layer_UX.md` Stage 1 has the composer waiting on the Claude API while `.claude/agents/reviewer.md` check 1 makes that a correctness finding. Posted as `exchange/open/2026-09-19-002` to `spec-reader` and open. Three further A-1 blockers found before any code: the focus ring `rgba(184,115,42,0.08)` has no token and `tokens.css` is generated from the design spec, so it cannot be hand-added; Stage 1 copy carries three em dashes that the voice hook will block on the way into `strings.ts`; and Stage 2.5 is absent from the Discourse Layer UX spec though A-3 cites it, the Delta Mechanic spec assumes it and root `CLAUDE.md` locks it at ten percent. Tooling: the `dialecta-local-research` MCP server did not connect this session, so sources were read through WebFetch and summarised by hand. The server binary and Ollama both answer when probed directly from the worktree, so the failure is the MCP connection. Separately `research_file` cannot file for a team agent at all, it accepts only `treasurer`, `designer` and `philosopher`, so the team half of `/dialecta-research` has no tool behind it. Next: `spec-reader` answers `2026-09-19-002`, then the Stage 2.5 gap.
- 2026-09-19 (designer training), branch `claude/jolly-dijkstra-03d6e7`: three research sprints trained the `designer` advisor from zero. Nineteen sources filed in `council/designer/research/`, 23 standing positions in `positions.md`, none `(unsourced)`. Full record in `council/log/sprints/2026-09-19-designer.md`. Three findings other agents need. **A-1:** no filed precedent gates the act of writing a first comment; Discourse gates reach, Stack Overflow gates authority, Community Notes gates publication, MetaFilter gates identity and time, and none gates length. **P0-4:** Supabase's own docs say the built-in email provider sends 2 per hour and "will refuse to deliver messages to addresses that are not part of the project's team", so magic link sign-up cannot work at cutover without custom SMTP, which is in neither the backlog nor ADR-002. **A-5:** six measured contrast and colour-vision failures in the locked tier tokens, reproducible from `council/designer/research/tier-palette-audit.py`; Heat badge text is 1.96:1 where 4.5:1 is required. Also found: `dpenn1000/trinity-platform` carries a design system whose spacing standard and colour law solve two of these in Dan's own idiom, and `dpenn1000/dialecta-api` carries five article endpoints this repo lacks, including `aesthetic-suggest.js`. Four records opened: `exchange/open/2026-09-19-002` through `-005`. Nothing was written outside `council/` and `exchange/` except this line. Next: the council has never convened; run `/dialecta-council` on P0-D2 once `treasurer` and `philosopher` have filed.
- 2026-09-19 (decider training): `decider` trained on branch `claude/recursing-yalow-ed3460`, four commits, 16 files, gates green (typecheck clean, 25 tests, voice check zero hard hits). Research sprint filed five notes to `team/decider/knowledge/`; all five seeded leads verified against primary sources, four new leads added, `practices.md` from five rows to fourteen. P0-D2 framed at `council/log/2026-09-19-p0-d2-login-methods.md`; advisors deliberately not run, being untrained. `exchange/open/2026-09-19-001` answered: recommends adopting the live schema paired with branching for staging, waiting on one question to Dan. First `vote` opened at `-002` with ballots unfilled for the same reason. Two frames written from Dan's direction: `2026-09-19-advisor-mandates.md`, which carries paste-ready charter drafts turning all three seats generative rather than guardrail, and `2026-09-19-council-composition.md`, which recommends no cap on the roster with every seat running every debate. Findings: the ADR template is missing the condition a decision rests on (recommend `## Holds while` from ADR-004 forward); five backlog decisions are Open, not four, B-D1 was missed everywhere; ADR-002 already chose magic link plus Google, so P0-D2 is only the sign-up gate; monetization is Project Brief question 7, miscited as 8 in the council skill and the treasurer charter. No spec, skill, charter or code file touched. Handoff to lead: `exchange/open/2026-09-19-003`. Next: Dan answers the evaluating question and the roster rules, then both frames become ADR-004 together.
- 2026-09-19 (philosopher training): first research sprint for the `philosopher` advisor, on branch `claude/vibrant-montalcini-721b8c`. Eleven sources verified and filed in `council/philosopher/research/`, none dead; eight standing positions P-1 to P-8 in `council/philosopher/positions.md`; sprint record in `council/log/sprints/2026-09-19-philosopher.md`. The sprint checked the charter's four named biases for the classification card instead of assuming them: reactance holds as stated, in-group signaling is the weaker half of a pair behind out-group animosity, the audience effect belongs to the Contrast Strip rather than the card, and loss aversion around tier is contested inside its own literature. Three disagreements recorded for Dan: the Classification Engine Specification overstates the commenter message as the primary behavior-change mechanism (P-3), the permanent public Contrast Strip will bias self-declaration downward and corrupt the 15 percent weight it carries (P-4), and the source thesis needs one amendment because the environment wins by distraction rather than by beating stated values (P-8). Three founding rules came out supported rather than dented. P-7 takes P0-D2 on consent grounds: the live question is what the Pact discloses before the first comment, magic link ahead of Google, and invite-only only as a time-boxed capacity measure. Open: blindspot `exchange/open/2026-09-19-002` to designer and treasurer. Note for the next session: `dialecta-local-research` failed to connect at session start, a race with the worktree checkout rather than a broken server, and was driven over a stdio bridge instead; retry the MCP connection before assuming it is down. Next: `/dialecta-research` for `treasurer` and `designer`, then `/dialecta-council` on P0-D2 with all three filed.
- 2026-09-19 (migrator training, env, RLS): `2026-09-19-001` is answered. The two September migrations were written without knowledge of the live database: the scaffold came from a Cowork chat as a zip, its own install handoff tells Dan to `db push` into a new project, root `CLAUDE.md` then described Supabase as four tables and Live, no session on studio-pc wrote the SQL, and the live project was first read 35 minutes after the scaffold was committed. On that record's logic the repo should adopt the live schema. Measured the live RLS surface: nine tables are closed to the anon key, six are fully public, so `028_pre_launch_security_hardening` did real work. **Defect: every column on live `profiles` is readable without authentication, including `is_admin`.** A-5 renders empty for logged-out visitors. Env files on studio-pc are loaded and all three keys verified against live. Added `scripts/check-env.mjs` and a narrow Bash allow rule. Filed seven migrator knowledge notes, `team/migrator/p0-2-runbook.md`, and exchange `2026-09-19-002`. Nothing written to the live database; `supabase/migrations/` untouched. Full detail: `dialecta-handoff-2026-09-19-env-and-rls.md`. Next: close `2026-09-19-001`, then fix the `profiles` exposure.
