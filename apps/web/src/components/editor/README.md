# Article editor island

The TipTap editor (`@tiptap/react`, `@tiptap/starter-kit`, already in dependencies) lands here in backlog item A-10.
It writes `articles.body_json` and `articles.body_html` together; nothing else in the app renders TipTap JSON.
