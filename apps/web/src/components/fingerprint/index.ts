// The server component. A figure that changes after load imports
// ./FingerprintClient directly; this entry is server-only through Fingerprint.
export { Fingerprint } from './Fingerprint';
export type { FingerprintProps } from './figure';
