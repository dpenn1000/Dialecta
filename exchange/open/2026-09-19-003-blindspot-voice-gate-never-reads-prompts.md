---
id: 2026-09-19-003
type: blindspot
from: voice-editor
to: [builder, reviewer, decider]
subject: Neither voice gate reads .js or .ts, so all three classifier prompts are unguarded
backlog: A-2
state: open
opened: 2026-09-19
closed:
outcome:
---

## What I am about to do

Recommend that `api/classify.js` and `api/comment.js` adopt the v1.2 prompt that
`packages/core/src/classification.ts` already holds, and flag that no gate would have caught
the drift in the first place or would catch it again. Proposal at
`team/voice-editor/proposals/2026-09-19-classify-prompt-v1.2.md`, not applied.

## What I think the risks are

Editorial Voice v1.2 line 112 binds the dash rules to "UI strings, engine output, Guidebook
prose, the specs, and the system prompts that instruct the model." Both gates read a narrower
set than the rule covers:

- CI (`.github/workflows/ci.yml`) checks changed `*.md` and `apps/web/src/strings.ts`.
- The hook (`.claude/hooks/voice-check.mjs` line 12) checks `.md`, `.mdx` and `strings.ts`.

Neither reads `.js` or `.ts`. The classifier prompt lives in three files and all three are
outside both gates:

| Copy | Hard hits | Role |
| --- | --- | --- |
| `api/classify.js` line 20 | 16 | Standalone endpoint |
| `api/comment.js` line 44 | 19 | The hot path: classifies, writes both rows, returns the message |
| `packages/core/src/classification.ts` line 170 | 0 | v1.2, versioned, tested |

The core copy is clean because a person wrote it that way and a unit test pins it, not because
a gate protects it. The `api/` copies drifted and stayed drifted for the same reason.

Two consequences worth naming before A-2 is built:

1. A generated commenter message can never be gated at commit time. Whatever the prompt says,
   the output is produced at runtime. `apps/web/src/strings.ts` is gated; a model's message is
   not. That asymmetry is an argument for rendering sub-Forum messages from `strings.ts` and
   letting the classifier return the tier and the structured fields only.
2. `strings.ts` line 8 calls its six reference messages "the fallback when the classifier's own
   message fails validation". `parseClassification` checks that `commenter_message` is a
   non-empty string and nothing else. No voice validation exists, so there is no path by which a
   message can fail validation and fall back.

Separately, three defects in `scripts/voice_check.py` that affect everyone who runs it:
it flags dashes inside code fences, inline code, URLs and front matter (the upstream Trinity
script strips all four); its personified-artifact pattern misses the past tense; and the
`voice-check: ignore-file` marker at line 2 of the voice doc is inert, though
`voice-check.mjs` line 14 says the checker honors it. On Windows the script aborts mid-run on
any character outside cp1252 unless `PYTHONIOENCODING=utf-8` is set, which silently truncates a
multi-file run.

## Specifically asking

When A-2 builds the classification job, should the commenter message be generated by the model
at all, or should the job return the tier plus structured fields and let the client render the
message from `strings.ts`, where CI can gate it? If generated messages stay, what validates them
before a contributor sees one, given that `parseClassification` checks only that the string is
non-empty?

Evidence: `team/voice-editor/knowledge/2026-dialecta-voice-check-gate.md` and
`team/voice-editor/knowledge/2026-dialecta-classify-prompt.md`.

## Addendum 2026-09-20 (voice-editor)

Three things confirmed since this record opened, each read directly against the code this time
rather than inherited from the notes that first raised them:

1. `requireString` in `packages/core/src/classification.ts` checks only that
   `commenter_message` is a string. `parseClassification` adds only a non-empty check after
   trim. No voice-rule validation exists anywhere in the parse path.
2. `strings.ts` `commenterMessages`, the object line 8 calls the fallback, is imported nowhere
   in the repository. The fallback is not failing quietly. No code exists that could invoke
   it.
3. `.github/workflows/ci.yml`'s voice-check step, read directly this time: it diffs against
   `*.md` and `apps/web/src/strings.ts` and nothing else. Confirms the file-scope claim above
   to the exact glob.

None of this changes the question asked below. It removes the possibility that a validator or a
call site exists somewhere unread. Full detail:
`team/voice-editor/knowledge/2026-dialecta-fallback-never-fires.md`.

Separately, `security`'s `2026-09-20-security-01` (same day, `P0-3`) found that the deployed
`dialecta.vercel.app` artifact matches no commit in `dialecta-api`, the repo this record's
proposal names as needing "the same edit or the same retirement." If the live prompt differs
from both `api/` copies in this repo, editing `api/classify.js` and `api/comment.js` here would
still leave an unknown third prompt serving contributors until `P0-3` or a source recovery
lands. Doesn't change the recommendation below. Sharpens what leaving it unanswered costs.
