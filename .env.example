# Copy to .env.local (Vercel dev) (never commit real values)
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# One-time Ghost import (scripts/import-ghost.mjs). Ghost is not a runtime
# dependency; these are only read by that script. Also needs SUPABASE_URL and
# SUPABASE_SERVICE_KEY above.
# GHOST_CONTENT_URL=https://dialecta.mymagic.page
# GHOST_CONTENT_KEY=your-content-api-key
