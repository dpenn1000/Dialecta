node_modules/
.env
.env.*
!.env.example
.vercel/
.DS_Store
Thumbs.db
.next/
supabase/.temp/
*.tsbuildinfo
.turbo/
tools/local-research/.index/
_to_delete/
council/log/sprints/*.log

# Credentials. Never commit these; they do not belong in the repo at all.
*.token
*.tolken
*.pem
*credentials*.json

# Recovered from a Vercel deployment artifact. Read it, compare it, promote nothing blind.

# NSSM service logs for the council API
.committee-logs/
.council-logs/

# Service security descriptor backup, written by grant-service-control.ps1
scripts/.service-sd-backup.txt

# Ghost theme, exported 2026-09-20. Quarantine, same rule as _recovered/.

# _recovered/, _recovered-next/ and _theme/ are deliberately NOT ignored.
# They are recovered evidence and they are committed so one laptop failure cannot
# lose them again. The quarantine is a rule, not a gitignore entry: read them, cite
# them, promote nothing blind. See docs/RECOVERED.md.
