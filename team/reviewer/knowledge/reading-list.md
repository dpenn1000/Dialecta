# Reading list

Leads, not facts. Every entry below is a lead to verify: confirm the source exists and says
what this line claims before filing a note on it. A lead that turns out to be wrong or missing
is marked `dead` with the reason, which is a result worth keeping.

States: `todo`, `filed`, `dead`.

| State | Lead | Why this agent needs it |
| --- | --- | --- |
| filed | Postgres row-level security: `USING` against `WITH CHECK`, and how multiple policies combine | Check 2 is the one that ships a real breach if it is wrong. The two clauses guard read and write and are easy to confuse. Filed as [2026-postgresql-create-policy](2026-postgresql-create-policy.md). Verified against PostgreSQL 18.6. The lead was right and understated: the larger hazard is that RLS is row level only, so neither clause restricts columns |
| filed | The Supabase RLS guide, including policy performance and the `auth.uid()` pattern | Every policy this repo writes uses it. Knowing the cost shape stops a correct policy that times out. Filed as [2026-supabase-row-level-security](2026-supabase-row-level-security.md). Verified. Carries the `bypassrls` qualifier the lead did not anticipate: the secret key bypasses RLS only when the request carries no user token |
| filed | Postgres `SECURITY DEFINER` functions and default execute grants | A definer function with a public execute grant is a hole that reads as normal code. Worth a note with a concrete example. Filed as [2026-postgresql-security-definer](2026-postgresql-security-definer.md). Verified against PostgreSQL 18.6, including the single-transaction create-and-revoke example. No definer function exists in this repo yet, so the note is a checklist held in advance |
| filed | Next.js: what crosses into the client bundle, `NEXT_PUBLIC_` semantics, and server action authorization | The service-role key reaching a client component is the failure this check exists to catch. Filed as [2026-nextjs-environment-variables](2026-nextjs-environment-variables.md). Partly verified. The environment variable half is confirmed against Next.js 16.3.5. The server action authorization half is a different page and was not read, so it is carried below as its own lead |
| filed | OWASP ASVS, the access control chapter | A checklist written by people who have seen the failures, against a review list written from first principles here. Filed as [2025-owasp-asvs-authorization](2025-owasp-asvs-authorization.md). Verified with a correction: the chapter is `V4 Access Control` in ASVS 4.0 and was renamed to `V8 Authorization` in 5.0.0, the current stable release, dated May 2025 |
| filed | Postgres column-level privileges: `GRANT UPDATE (column)` and how a column grant composes with a row policy | The gap behind two of the three blockers in the PR 3 review. Filed as [2026-postgresql-column-privileges](2026-postgresql-column-privileges.md). Verified, and the lead understated it: a column-level revoke against a table-level grant is a silent no-op, so the obvious fix for B2 would have reviewed as correct and done nothing. Carries the worked remedy. The half about composing with a row policy is not on that page and is carried below |
| filed | Supabase default grants to `anon` and `authenticated` in the `public` schema | The blockers assume `authenticated` already holds insert and update on every new table, so RLS is the only gate. Filed as [2026-supabase-default-grants](2026-supabase-default-grants.md). Confirmed, and with a date: the defaults are being retired, existing projects on 2026-10-30, but tables created before then keep their grants, so B2 does not expire |
| filed | OWASP ASVS 5.0.0 chapter V8, levels 1 to 3, end to end | Only 8.2.2 and 8.3.1 had been read. Done: thirteen requirements across four sections, folded into [2025-owasp-asvs-authorization](2025-owasp-asvs-authorization.md) and into rows 12 and 13 of [review-checklist](review-checklist.md). The unexpected result is 8.1.1 and 8.1.2, a documentation requirement Dialecta does not meet at all |
| filed | Postgres row security: the `ddl-rowsecurity` page, on whether RLS and the privilege system are independent checks | The two-layer framing was sourced from Supabase and not from Postgres. Section 5.9 supplies it: policies apply "In addition to the SQL-standard privilege system". Folded into [2026-postgresql-create-policy](2026-postgresql-create-policy.md). The lead delivered more than it promised: table owners bypass RLS by default unless `FORCE ROW LEVEL SECURITY` is set, which is why a definer function owned by `postgres` skips RLS on every table it touches |
| filed | PostgREST behavior when a role lacks a column privilege: the error code and whether the failure is legible | Confirmed and it is not fully legible. `42501` surfaces "as 401 or 403 errors" and has five causes including RLS denial, so a blocked write and a policy bug look identical from the browser. Folded into [2026-postgresql-column-privileges](2026-postgresql-column-privileges.md). Does not undo the remedy; it argues for landing grants and policies in one commented migration |
| filed | Sanitizing rich-text HTML before render: what TipTap guarantees about its own output, and where DOMPurify belongs in a server component | Filed as [2026-cure53-dompurify](2026-cure53-dompurify.md), the remedy for blocker B1. Answer is both ends, for different reasons, because the write path is not the only way the column gets written. The TipTap half was not read and is carried below |
| filed | Content Security Policy in Next.js: the `headers()` config and the nonce pattern | Filed as [2026-nextjs-content-security-policy](2026-nextjs-content-security-policy.md), and it corrected a claim in the PR 3 review rather than confirming it. The `next.config.js` recipe sets `script-src 'self' 'unsafe-inline'`, which permits the exact inline handler B1 uses |
| filed | Server action and route handler authorization in Next.js App Router | The half of the Next.js lead that was not read. Filed as [2026-nextjs-server-actions-authorization](2026-nextjs-server-actions-authorization.md). Confirmed and sharper than expected: the framework performs zero authorization anywhere, and its own docs rule out both a parent layout and Proxy as sufficient. New checklist row 13b |
| filed | The Supabase database linter rule set, starting with `function_search_path_mutable` | `supabase/CLAUDE.md` makes `supabase db lint` a gate. Filed as [2026-supabase-database-linter](2026-supabase-database-linter.md), all 30 rules. The rule set covers checklist rows 4, 5 and part of 9; nothing in it inspects a column-level grant, so rows 6 and 7, the two rows that actually found blockers, have no tooling behind them at all |
| filed | Supabase, "Postgres Roles and Privileges", the engineering blog post | Found while reading the grants question. Filed as [2026-postgresql-public-default-privileges](2026-postgresql-public-default-privileges.md), paired with the PostgreSQL primary source it was reasoning about. Settles that `PUBLIC` gets no default privilege on a table, only on a function, which closes a gap the B2 remedy left implicit |
| filed | The malleability window: the 60 minute edit and re-classification rule, in `docs/Dialecta_Axis_Mapping_v1.md` universal rule 6 and wherever it is specified in full | Filed as [2026-axis-mapping-malleability-window](2026-axis-mapping-malleability-window.md). Rule 6 is specified in exactly one place, not two; the Tuning Engine spec only charts it. `_recovered/api/_axis-mapping.js` carries a working, service-role-based implementation of the rule, which sharpens the fourteenth finding's spec tension without settling it |
| filed | TipTap: what its HTML output guarantees, and whether `generateHTML` on the server is safe against a crafted `body_json` | Filed as [2026-tiptap-mergeattributes-xss](2026-tiptap-mergeattributes-xss.md). Answer is no: a real, current advisory (GHSA-cp6q-959q-f8rh, verified against GitHub's own registry) shows a crafted attributes object reaching `mergeAttributes()` produces executable DOM attributes regardless of schema validation. This repo's pinned `@tiptap/core` (3.31.3) is above the patch line, verified in `package-lock.json`, so not currently exploitable, but only because the dependency arrived after the fix shipped |
| filed | Whether `FORCE ROW LEVEL SECURITY` belongs on Dialecta's tables, and what Supabase's own migrations assume about the owner bypass | Filed as [2026-postgresql-force-rls-supabase](2026-postgresql-force-rls-supabase.md). Answer is no, and it is a closed no rather than an open hardening question: on Supabase the owner role `postgres` carries `BYPASSRLS` directly, and `FORCE` has no effect on a `BYPASSRLS` role. Row 9b revised rather than upgraded |
| todo | jsdom release history and known parser bugs, enough to say what "keep it current" costs in practice | The DOMPurify README makes the jsdom version part of the security boundary. A remedy that depends on a transitive dependency staying fresh needs a sentence about how that is kept true |
| todo | `pgrls`, a Postgres RLS static analyzer and pytest plugin found this sprint via the GitHub tool search | 67 lint rules for tenant and per-user row-scoping bugs, 20 auto-fixable, plus a policy-diff command for CI gating. Directly answers the reading list's own gap: nothing in this repo's tooling checks a column-level grant. Worth a trial once a local Supabase stack exists, alongside `supabase db lint` per `## Next three` item 3 in `brief.md`. `pgrls/pgrls` on GitHub, MIT, 26 stars, pushed 2026-09-17, young (created 2026-04-24), single-repo bus-factor risk |
| todo | OWASP ASVS 5.0.0, the Validation, Sanitization and Encoding chapter | Found while verifying the TipTap advisory. The Authorization chapter (V8) is filed and drives check 2; this repo's other named blocker, B1, is an output-encoding failure, and nothing here has read the chapter ASVS itself devotes to that category. Likely sharpens checklist row 14 the way V8 sharpened rows 6 and 7 |

*Sprint of 2026-09-19, three passes in one day, plus a fourth on 2026-09-20. Seeded with five
leads, all five filed, none dead. Eighteen filed in total now, three open, down from seven
open this morning after six were worked and two new ones were added. Every pass has added
leads as fast as it has closed them, which is the point. Three leads have corrected a claim
rather than confirming one: the OWASP chapter number, the CSP benefit in finding S7, and now
`FORCE ROW LEVEL SECURITY`, which closed as a settled no rather than the open hardening
question it was filed as.*
