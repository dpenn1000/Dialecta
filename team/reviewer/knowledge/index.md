# Knowledge index

Leads live in `reading-list.md`; a sprint files them here, one file per source,
named `YYYY-<author>-<slug>.md` with citation, summary, and what it implies for a
named Dialecta surface or a named practice.

| File | Source | Implies for |
| --- | --- | --- |
| [2026-supabase-default-grants.md](2026-supabase-default-grants.md) | Supabase, "Securing your API"; changelog 45329 | Check 2, the grant layer. New tables in `public` get insert, update and delete for `anon` and `authenticated` by default. Being removed, but existing tables keep their grants |
| [2026-postgresql-column-privileges.md](2026-postgresql-column-privileges.md) | PostgreSQL 18.6, "GRANT" | Check 2. Column privileges are additive, so a column-level revoke against a table-level grant is a silent no-op. Carries the worked remedy for blocker B2 |
| [2026-postgresql-create-policy.md](2026-postgresql-create-policy.md) | PostgreSQL 18.6, "CREATE POLICY" | Check 2. RLS is row level only, so an owner-writes-own-row policy says nothing about columns. A read failure is silent, a write failure raises |
| [2026-supabase-row-level-security.md](2026-supabase-row-level-security.md) | Supabase, "Row Level Security" | Check 2. `service_role` carries `bypassrls`, and only when the request has no user token. Policy shape: one per operation, `to` clause, `(select auth.uid())` |
| [2026-postgresql-security-definer.md](2026-postgresql-security-definer.md) | PostgreSQL 18.6, "CREATE FUNCTION", writing SECURITY DEFINER safely | Check 2. A definer function needs `pg_temp` last in `search_path` and an explicit revoke from `PUBLIC`, both in one transaction |
| [2026-nextjs-environment-variables.md](2026-nextjs-environment-variables.md) | Next.js 16.3.5, "How to use environment variables" | Check 2. `NEXT_PUBLIC_` is a build-time inline and the prefix is the entire check. Dynamic lookups do not inline |
| [2025-owasp-asvs-authorization.md](2025-owasp-asvs-authorization.md) | OWASP ASVS 5.0.0 (May 2025), "V8 Authorization", full chapter | Check 2. Object level (8.2.2) and field level (8.2.3) are separate requirements at separate levels. 8.1.1 and 8.1.2 ask for documentation Dialecta does not have |
| [2026-cure53-dompurify.md](2026-cure53-dompurify.md) | Cure53, DOMPurify README | The remedy for blocker B1. Sanitize at write time and at read time, for different reasons. The pinned jsdom version is part of the boundary |
| [2026-nextjs-content-security-policy.md](2026-nextjs-content-security-policy.md) | Next.js 16.3.5, "How to set a Content Security Policy" | Check 2, second layer. The `next.config.js` recipe uses `unsafe-inline` and does nothing about an injected `onerror`. Only the nonce and SRI forms bite |
| [2026-nextjs-server-actions-authorization.md](2026-nextjs-server-actions-authorization.md) | Next.js Docs 16.3.5, "How to implement authentication" | Check 2, application layer. Server Actions and Route Handlers get zero automatic authorization; a parent layout and Proxy are both explicitly insufficient. New checklist row 13b |
| [2026-supabase-database-linter.md](2026-supabase-database-linter.md) | Supabase Docs, "Database Linter", 30 rules | Check 2. Covers checklist rows 4, 5 and part of 9. Confirms nothing in the linter inspects a column-level grant, so rows 6 and 7 have no automated coverage at all |
| [2026-postgresql-public-default-privileges.md](2026-postgresql-public-default-privileges.md) | PostgreSQL 18.6 "Privileges"; Supabase, "Postgres Roles and Privileges" | Check 2. `PUBLIC` gets no default privilege on a table, only `EXECUTE` on a function. Closes a gap the B2 remedy left implicit and doubles the sourcing behind the `SECURITY DEFINER` revoke |
| [2026-axis-mapping-malleability-window.md](2026-axis-mapping-malleability-window.md) | `docs/Dialecta_Axis_Mapping_v1.md` rule 6; `docs/Dialecta_Tuning_Engine_Spec_v1.md`; `_recovered/api/_axis-mapping.js` (quarantine) | Check 3. The 60 minute window is specified once, not twice. The quarantined file shows a shipped, service-role-based implementation, which sharpens but does not settle the append-only spec tension |
| [2026-tiptap-mergeattributes-xss.md](2026-tiptap-mergeattributes-xss.md) | GitHub Security Advisory GHSA-cp6q-959q-f8rh | Check 2, render layer. A real, current advisory shows `generateHTML` unsafe against a crafted `body_json` independent of schema validation. Not currently exploitable here; verified against the pinned version in `package-lock.json` |
| [2026-postgresql-force-rls-supabase.md](2026-postgresql-force-rls-supabase.md) | PostgreSQL 18.6 "ALTER TABLE" | Check 2. `FORCE ROW LEVEL SECURITY` would not help on Supabase specifically, since the owner role carries `BYPASSRLS` directly and `FORCE` does not affect that attribute. Row 9b revised |
| [2026-recovered-axis-mapping-comparison.md](2026-recovered-axis-mapping-comparison.md) | `_recovered/api/_axis-mapping.js` (quarantine) against `packages/core/src/axis-mapping.ts` and the spec | Check 3. Confirms blocker B3 axis by axis and adds two structural findings a weight change cannot fix: Reach has no topic-history input, Consistency cannot accrue as written |
| [review-checklist.md](review-checklist.md) | This agent, from the mandate and the notes above | The named failure modes behind check 2, so it is a list rather than a category. Eighteen rows, grant layer first |

*Note: `review-checklist.md` is written by this agent rather than filed from a source. It is listed here so the index is the whole folder.*
